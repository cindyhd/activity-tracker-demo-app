import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function home() {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: "#DCF2F1",
      }}
    >
      <Text>home bottom tab bar</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
